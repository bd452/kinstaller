[toolchain]
channel = "1.96.1"
targets = [
    "armv7-unknown-linux-gnueabihf", # kindlehf (koxtoolchain device builds)
    "armv7-unknown-linux-gnueabi",   # kindlepw2 (koxtoolchain device builds)
    "armv7-unknown-linux-musleabihf",  # kindlehf (--musl UI-only smoke tests)
    "armv7-unknown-linux-musleabi",    # kindlepw2 (--musl UI-only smoke tests)
]
